/*
// CROSS_SURFACE_PARITY_EXEMPT: hidden in block
*/
export const afterBlock = true;

/**
 * // CROSS_SURFACE_PARITY_EXEMPT: hidden in jsdoc
 */
export const afterJSDoc = true;

/* // CROSS_SURFACE_PARITY_EXEMPT: hidden single-line block */
export const hiddenSingleLineBlock = true;

/* closed block */ // CROSS_SURFACE_PARITY_EXEMPT: legit reason
export const afterSameLineBlockClose = true;

/* unclosed block starts here
// CROSS_SURFACE_PARITY_EXEMPT: hidden after unclosed block
