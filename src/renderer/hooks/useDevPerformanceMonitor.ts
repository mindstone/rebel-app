/**
 * Deprecated compatibility module.
 *
 * This file only exists so existing imports — in particular the
 * `formatLongTaskAttribution` test fixture — continue to resolve.
 */

export { formatLongTaskAttribution } from './performanceMonitor/scrubAttribution';
