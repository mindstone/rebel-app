import type {
  FlatFileEntry,
  SearchResult,
  SkillMeta,
} from '@renderer/utils/librarySearch';

export type FlatLibraryEntry = FlatFileEntry;
export type LibrarySearchResult = SearchResult;
export type LibrarySkillMeta = SkillMeta;
