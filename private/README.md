# Private directory

This directory holds Mindstone-internal material in the private development repository, including credential providers and internal documentation. Those files are excluded from the public mirror.

Public and self-hosted builds resolve connector credentials from environment variables and public configuration instead. See the repository's OSS setup and connector documentation for the supported public paths.
